<div id="<%= dom_id upload %>">
  <p>
    <strong>Title:</strong>
    <%= link_to upload.title, url_for(upload.file), target: :blank %>
  </p>

  <% unless upload.description.blank? %>
    <p>
      <strong>Description:</strong>
      <%= upload.description %>
    </p>
  <% end %>
</div>
